const {getData, RequestType} = require("../../requests/Request");
const Settings = require("../../settings/Settings");
const {minigameIndex} = require("../../utils/MinigamesUtils");

class Test {

    ett() {

        let correctMatches;

        correctMatches.add(card);
        console.log(correctMatches.size());
    }
}

module.exports = Test;